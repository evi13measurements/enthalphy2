module scratcharr
   use matrix_module
!   integer, save              :: scratch_size    ! SIZE(scratch_area)
!   real(realk), save, pointer :: scratch_area(:) ! pointer to DALTON's work space
!   integer, save              :: coulomb_mem_req ! Memory requirements for the Coulomb build
!   real(realk), save, pointer :: coulomb_wrk(:)
!   integer, save              :: dft_mem_req     ! Memory requirements for the DFT build
!   real(realk), save, pointer :: dft_wrk(:)
end module scratcharr
